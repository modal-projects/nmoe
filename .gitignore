# =============================================================================
# nmoe: gitignore (public repo hygiene)
# =============================================================================

# Python bytecode / caches
__pycache__/
*.py[cod]
*.pyd

# Native build outputs
*.o
*.a
*.so
*.ptx
*.cubin
*.dylib

# Python packaging artifacts
build/
dist/
*.egg-info/

# Virtualenvs
.venv/
venv/

# Local / editor tooling
.claude/
.vscode/
.idea/

# Local env/config (never commit secrets)
.env
.env.*
*.env
*.local.*

# Allow committing template/example env files.
!.env.example
!nviz/.env.example

# nviz local env (contains local endpoints)
nviz/.env.local

# Internal-only ops notes
AGENTS.md.local

# Internal-only debug/handoff docs (do not publish)
*_handoff*.md
PIPELINE_TEST_RESULTS.md

# Third-party working trees (not shipped; only patches ship)
third_party/nvshmem/
third_party/flash_attn/

# nviz build outputs
nviz/node_modules/
nviz/.next/
nviz/out/
nviz/.turbo/
nviz/tsconfig.tsbuildinfo

# Profiling artifacts (never commit)
*.nsys-rep
*.ncu-rep
*.qdrep
*.sqlite
*.sqlite3
*.duckdb

# Local scratch / snapshots
tmp/

# OS junk
.DS_Store
Thumbs.db
